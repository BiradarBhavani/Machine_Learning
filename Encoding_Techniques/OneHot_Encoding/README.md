# One-Hot Encoding

Creates a new binary (0/1) column per category. Best for nominal
(unordered) features with low cardinality (<~15 categories).

## Structure
```
OneHot_Encoding/
├── onehot_encoding.ipynb
└── data/loan_data.csv
```
