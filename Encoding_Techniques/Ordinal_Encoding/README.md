# Ordinal Encoding

Like Label Encoding but you define the rank order explicitly, and it
works on multiple columns at once. Best for features with a genuine
order (education level, size, rating).

## Structure
```
Ordinal_Encoding/
├── ordinal_encoding.ipynb
└── data/loan_data.csv
```
