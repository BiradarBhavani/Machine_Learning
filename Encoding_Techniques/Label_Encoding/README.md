# Label Encoding

Converts each category to a single integer. Works on one column at a
time — best for the target column or a binary categorical feature.
Avoid on nominal features with 3+ categories (implies false order).

## Structure
```
Label_Encoding/
├── label_encoding.ipynb
└── data/loan_data.csv
```
